import theme from "@chakra-ui/core/dist/theme"

// Let's say you want to add custom colors
export default {
  ...theme,
  fonts: {
    body: "Rubik, -apple-system, BlinkMacSystemFont, Helvetica, sans-serif",
    heading: "Rubik, -apple-system, BlinkMacSystemFont, Helvetica, sans-serif",
    mono: "monospace",
  },
}
